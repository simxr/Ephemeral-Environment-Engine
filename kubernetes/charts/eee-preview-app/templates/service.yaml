apiVersion: v1
kind: Service
metadata:
  name: {{ include "eee-preview-app.fullname" . }}
  labels:
    {{- include "eee-preview-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eee-preview-app.selectorLabels" . | nindent 4 }}
