{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "eee-preview-app.serviceAccountName" . }}
  labels:
    {{- include "eee-preview-app.labels" . | nindent 4 }}
{{- end }}
